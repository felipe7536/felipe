''' Esse programa foi criado com objetivo  de calcular a soma de vetores a,b em N dimensões
aluno:FELIPE RODRIGUES RIBEIRO
DRE:119052031
curso:Matemática Aploicada
disciplina: Tópicos de Mat.'''

vetor_1=[]
vetor_2=[]
somavetores=[]
print('Função para somar vetores a e b em N dimensões')
print('')
n=int(input('Defina as N dimensões do seu vetor A e B: '))
print('')
for i in range(n):
    if i!=n:# loop 'for' criado para N dimensões
        i+=1
        a=float(input('Defina a coordenada do seu vetor A na '+str(i)+'ª dimesão (caso o vetor não chegue até esta dimensão digite 0): '))
        b=float(input('Defina a coordenada do seu vetor B na '+str(i)+'ª dimesão (caso o vetor não chegue até esta dimensão digite 0): '))
        t=a+b
        vetor_1.append(a)
        vetor_2.append(b)
        somavetores.append(t)#soma vetorial colocada  numa lista vazia
    if i==n:    
       break
print('')
print('Essa é a soma dos vetores '+str(vetor_1)+' e '+str(vetor_2)+' nas '+str(n)+' dimensões: ')
print(somavetores)
